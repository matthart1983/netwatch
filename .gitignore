/target
/result
*.DS_Store

# JetBrains IDE config — machine-specific, shouldn't be tracked
.idea/
Roadmap*.md
SPEC-*.md
comparison.md
demo-linkedin.*
*.mov

# vhs intermediate output (we keep the screenshots, not the .gif)
screenshots/.vhs-trash.gif
